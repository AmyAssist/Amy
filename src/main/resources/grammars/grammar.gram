#JSGF V1.0;

/**
 * JSGF Grammar 
 */

grammar grammar;

public <wakeup> = ( amy );

<instruction> = ( open | start | get | search );
<keyword> = ( music  | command prompt | google | calendar | online);


public <command> = <instruction> <keyword>;